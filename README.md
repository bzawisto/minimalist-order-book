# minimalist-order-book
Clean and minimalist limit order book written in Rust

## Features
- Standard `Buy` and `Sell` matching
- Built with standard Rust
- Single-threaded with no lock overhead
- Price and quantity are represented as `Decimal`s



## Usage
Run `cargo run` to see the basic limit order sequence.
